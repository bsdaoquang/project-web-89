/** @format */

import React from 'react';

const ScoreComponent = ({ score }) => {
	return (
		<div>
			<h1 style={{ fontSize: 82, fontWeight: 'bold' }}>{score}</h1>
		</div>
	);
};

export default ScoreComponent;
